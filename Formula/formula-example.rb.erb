# typed: strict
# frozen_string_literal: true

# The auto-generated formula for '<%= name %>'
class <%= name.split(/-|_/).map(&:capitalize)*'' %> < Formula
  desc "<%= description %>"
  homepage "<%= homepage %>"
  version "<%= version %>"
  license "<%= license %>"
  on_arm do
    on_macos do
      url "<%= repository %>/releases/download/v<%= version %>/<%= name %>-aarch64-apple-darwin-v<%= version %>.tgz"
      sha256 "<%= sha256_aarch64_apple_darwin %>"
    end
    on_linux do
      url "<%= repository %>/releases/download/v<%= version %>/<%= name %>-aarch64-unknown-linux-gnu-v<%= version %>.tgz"
      sha256 "<%= sha256_aarch64_unknown_linux_gnu %>"
    end
  end
  on_intel do
    on_macos do
      url "<%= repository %>/releases/download/v<%= version %>/<%= name %>-x86_64-apple-darwin-v<%= version %>.tgz"
      sha256 "<%= sha256_x86_64_apple_darwin %>"
    end
    on_linux do
      url "<%= repository %>/releases/download/v<%= version %>/<%= name %>-x86_64-unknown-linux-gnu-v<%= version %>.tgz"
      sha256 "<%= sha256_x86_64_unknown_linux_gnu %>"
    end
  end
  def install
    bin.install "<%= name %>"
  end
  test do
    assert_equal "<%= version %>\n", shell_output("'#{bin}/<%= name %>' --version")
  end
end
